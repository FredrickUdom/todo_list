import { TodoStatusValidationPipePipe } from './todo-status-validation-pipe.pipe';

describe('TodoStatusValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new TodoStatusValidationPipePipe()).toBeDefined();
  });
});
